#!name=Shadowrocket 证书配置
#!desc=配置 MITM 证书和主机名，用于 HTTPS 流量解密
#!author=证书配置模块
#!arguments=启用证书:true, 证书内容:, 证书密码:Shadowrocket, 解密主机名:, 主机插入方式:%APPEND%

[MITM]
enable={{{启用证书}}}
ca-passphrase={{{证书密码}}}
ca-p12={{{证书内容}}}
hostname = {{{主机插入方式}}} {{{解密主机名}}}